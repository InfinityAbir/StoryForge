export const environment = {
  production: true,
  // Set this to your deployed backend's URL once it's live (e.g. Render web service),
  // then rebuild and redeploy the frontend. Must not have a trailing slash.
  apiBaseUrl: 'https://storyforge-wp99.onrender.com/api'
};
